# IA / editor
.claude
.aider*
.codex
.openclaude-profile.json

# Node
node_modules/

# Build outputs
dist/
src-tauri/target/

# Tauri store (dados locais do usuário, não devem ir pro repositório)
vault-config.json

# Env local (credenciais de desenvolvimento)
.env
.env.local

# Secrets / signing material
client_secret*.json
*.jks
*.keystore

# Pastas geradas por agentes de IA (artefatos acidentais)
path/
"Conteúdo do Arquivo `src/"
"Conteúdo do arquivo `src/"
"Conteúdo do Arquivo `src-tauri/"
